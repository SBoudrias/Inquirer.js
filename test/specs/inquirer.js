/**
 * Inquirer public API test
 */

var expect = require("chai").expect;
var sinon = require("sinon");
var _ = require("lodash");
var ReadlineStub = require("../helpers/readline");
var inquirer = require("../../lib/inquirer");


describe("Inquirer public API", function() {

  // Run prompts array

  // Run single prompt

  // Return answers in callback

  // Hierarchical prompts (`when`)

    // pass answers as parameter

    // continu if `when` returns true

    // don't run prompt if `when` returns false

    // Run async `when`

});
