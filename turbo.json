{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "tsc": {
      "dependsOn": ["^tsc"],
      "outputs": ["dist/**"],
      "inputs": ["../../package.json", "package.json", "src/*", "!**/*.test.ts"]
    },
    "test": {
      "dependsOn": ["^tsc"],
      "outputs": []
    }
  }
}
